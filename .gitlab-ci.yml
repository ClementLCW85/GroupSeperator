pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html public/
    - cp styles.css public/
    - cp app.js public/
    - cp "联合小组 2 - Full Name List (updated).json" public/
  artifacts:
    paths:
      - public
  only:
    - main
